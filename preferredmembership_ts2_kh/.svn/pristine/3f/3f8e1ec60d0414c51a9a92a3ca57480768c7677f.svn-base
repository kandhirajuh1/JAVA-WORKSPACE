/*package com.ck.rewards.test.service;

import java.sql.SQLException;
import java.util.ArrayList;
import java.util.List;
import org.junit.Before;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.MockitoAnnotations;
import org.mockito.runners.MockitoJUnitRunner;
import static org.mockito.Mockito.when;
import com.ck.rewards.dao.StoreDAO;
import com.ck.rewards.entity.Store;
import com.ck.rewards.service.StoreService;
import junit.framework.Assert;

@RunWith(MockitoJUnitRunner.class)
public class StoreServiceTest {
	@InjectMocks
	private StoreService storeService;
	@Mock
	private StoreDAO storeDAO;

	Store store = new Store();
	List<Store> expected = new ArrayList<Store>();
	List<Store> actual = new ArrayList<Store>();
	
	*//**
	 * 
	 *//*

	@Before
	public void setStoreInfo() {
		MockitoAnnotations.initMocks(this);
		String state_Id = "1";
		store.setStore_Name("Calvin Klein");
		store.setAddrline1("Allen Premium Outlets");
		store.setAddrline2("820 W Stacy");
		expected.add(store);
	}
		
		@SuppressWarnings("deprecation")
		@Test
		public void testGetStoresListById() throws ClassNotFoundException, SQLException {
			String state_Id = "1";
			store.setStore_Name("Calvin Klein");
			store.setAddrline1("Allen Premium Outlets");
			store.setAddrline2("820 W Stacy");
			expected.add(store);

		when(storeDAO.getStoresList(state_Id)).thenReturn((ArrayList<Store>) actual);
		Assert.assertEquals(expected, storeService.getStoreListByStateId(state_Id));
		}

	}

*/